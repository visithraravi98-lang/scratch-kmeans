# scratch-kmeans
This project focuses on understanding K-Means clustering deeply,not just using it.  Instead of relying on ready-made machine learning functions,the algorithm is implemented from scratch using Numpy. So every mathematical step is controlled and understood.
